/* 
  进入new目录
  命令行输入
    tsc -b src/server
    tsc -b src/server --verbose (带上编译信息)

    tsc -b src/client
    tsc -b src/client --verbose
    
    tsc -b test
    tsc -b test --verbose


  说明:
    -b 是build的简写

    tsc -b test --clean 清空编译文件
*/
