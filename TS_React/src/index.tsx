import React from 'react';
import ReactDOM from 'react-dom';

import Hello from './components/Hello';

ReactDOM.render(
    <Hello name="TypeScript React" />,
    document.querySelectorAll('.app')[0]
);
